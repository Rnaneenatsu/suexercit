import SwiftUI

struct ContentView: View {
    var body: some View {
        Rectangle()
            .fill(Color.blue) // Example usage of `fill` function
            .frame(width: 100, height: 100)
    }
}
